/**
 * `next-intl` is bundled for roadmap expansion (localized routes).
 * Runtime copy + RTL are handled via `LangContext` and `data/ui-strings.ts`
 * to match the project's lightweight bilingual toggle spec.
 */

export const supportedLocales = ["en", "ur"] as const;
